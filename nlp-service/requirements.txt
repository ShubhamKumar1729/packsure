# PackSure NLP service — runtime dependencies (Python 3.10+)
# Install: pip install -r requirements.txt
#
# NOTE: system dependency — Tesseract OCR must be installed separately:
#   Debian/Ubuntu: sudo apt-get install -y tesseract-ocr
#   macOS:         brew install tesseract
#   Docker image:  included already

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pillow>=10.2
pytesseract>=0.3.10
python-multipart>=0.0.9

# Optional but recommended: barcode detection for CV observations.
zxing-cpp>=2.2

# --- Only needed to SERVE a trained NER model (skip for pattern-extractor-only runs) ---
# Install with: pip install -r requirements-model.txt
# torch and transformers are intentionally NOT in the runtime list so the
# service can run lightweight (pattern extractor) without the ML stack.
